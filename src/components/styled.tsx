import styled from "@emotion/styled";

export const ChevronContainer = styled.div`
    display: flex;
    flex-direction: column;
    margin-left: 4px;
    cursor: pointer;
    svg:first-of-type {
        margin-bottom: 1px;
    }
`;